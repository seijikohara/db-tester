/**
 * Apache Cassandra wide-column database integration tests.
 *
 * <p>This package demonstrates that DB Tester works with Apache Cassandra, a distributed
 * wide-column NoSQL database, using the ING Bank Cassandra JDBC Wrapper. Cassandra uses CQL
 * (Cassandra Query Language), which has SQL-like syntax but different semantics for some
 * operations.
 */
@NullMarked
package example.database.cassandra;

import org.jspecify.annotations.NullMarked;
